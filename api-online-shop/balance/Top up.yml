info:
  name: Top up
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/api/v1/balance/topup"
  body:
    type: json
    data: |-
      {
        "amount": 5500
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

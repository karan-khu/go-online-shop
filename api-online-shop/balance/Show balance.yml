info:
  name: Show balance
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/balance/show"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

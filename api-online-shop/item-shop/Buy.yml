info:
  name: Buy
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/api/v1/item-shop/buy"
  body:
    type: json
    data: |-
      {
        "item_id": 4,
        "quantity": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

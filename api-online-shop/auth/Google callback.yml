info:
  name: Google callback
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/auth/google/callback"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

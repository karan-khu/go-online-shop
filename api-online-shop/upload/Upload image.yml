info:
  name: Upload image
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/api/v1/upload/image"
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - /Users/ka12m/Downloads/blc-heat-pen.jpeg
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

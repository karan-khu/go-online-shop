info:
  name: Sell
  type: http
  seq: 6

http:
  method: POST
  url: "{{BASE_URL}}/api/v1/items/sell"
  body:
    type: json
    data: |-
      {
        "item_id": 4,
        "quantity": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

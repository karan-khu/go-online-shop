info:
  name: Create
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/api/v1/items/create"
  body:
    type: json
    data: |-
      {
        "name": "Spell Tome",
        "description": "+40 พลังเวท",
        "price": 220,
        "picture": "http://localhost:8800/uploads/spell-tome.webp"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

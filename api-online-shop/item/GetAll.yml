info:
  name: GetAll
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/items?search_text=sword"
  params:
    - name: search_text
      value: sword
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

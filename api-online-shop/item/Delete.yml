info:
  name: Delete
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{BASE_URL}}/api/v1/items/delete/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

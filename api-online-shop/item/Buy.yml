info:
  name: Buy
  type: http
  seq: 5

http:
  method: POST
  url: "{{BASE_URL}}/api/v1/items/buy"
  body:
    type: json
    data: |-
      {
        "item_id": 4,
        "quantity": 4
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
